# Импорт файла в котором находятся классы
from classes import *

# Импорт библиотек
import time

# Вечный бесконечный цикл
while True:
    # Создаём экземпляр класса fulldates
    dddates = FullDates()
    dddates.printidates()
    time.sleep(60)